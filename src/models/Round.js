export default class Round {

    constructor(values, expectation, timestamp) {
        this.id = 0;
        this.values = values;
        this.expectation = expectation;
        this.timestamp = timestamp;
    }
    
}